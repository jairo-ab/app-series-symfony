{% extends 'base.html.twig' %}

{% block title %}Lista de Séries{% endblock %}

{% block body %}

    <a class="btn btn-info mb-3" href="/series/create">Adicionar</a>

    <ul class="list-group">
        {% for series in seriesList %}
            <li class="list-group-item">{{ series }}</li>
        {% endfor %}
    </ul>

{% endblock %}
